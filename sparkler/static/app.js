/* Sparkler ANPR Studio — light helpers shared across pages.
   Most reactivity lives inline with Alpine + HTMX in each template;
   this file holds anything that grows beyond a one-liner. */

window.sparkler = window.sparkler || {};
